name: Cron (Daily — HTTP)

on:
  schedule:
    - cron: '7 3 * * *' # daily 03:07 UTC
  workflow_dispatch: {}

concurrency:
  group: cron-daily-http
  cancel-in-progress: false

jobs:
  daily:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CLEARCOST_INTERNAL_API_URL: ${{ secrets.CLEARCOST_INTERNAL_API_URL }}
      CLEARCOST_TASKS_API_KEY: ${{ secrets.CLEARCOST_TASKS_API_KEY }}
      INTERNAL_SIGNING_SECRET: ${{ secrets.INTERNAL_SIGNING_SECRET }}
      MY_MFN_OFFICIAL_EXCEL_URL: ${{ secrets.MY_MFN_OFFICIAL_EXCEL_URL || '' }}
      PH_TARIFF_EXCEL_URL: ${{ secrets.PH_TARIFF_EXCEL_URL || '' }}
      UK_REMEDY_MEASURE_TYPES: ${{ vars.UK_REMEDY_MEASURE_TYPES || '552,551,695' }}
      EU_TARIC_REMEDY_TYPES: ${{ vars.EU_TARIC_REMEDY_TYPES || '' }}
      PRUNE_DAYS: ${{ vars.IMPORTS_PRUNE_DAYS || 90 }}
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.8'

      - name: Validate internal envs
        run: bun run internal-request -- --validate

      - name: FX daily (ECB)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/fx/daily --body '{}')
          echo "$OUT" | jq .
          FX_AS_OF=$(echo "$OUT" | jq -r '.fxAsOf // empty')
          if [ -z "$FX_AS_OF" ]; then
            echo "[fx:daily] missing fxAsOf in response"
            exit 1
          fi

      - name: VAT (OECD/IMF auto)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/vat/auto --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[vat:auto] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: EU Duties (TARIC — daily)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/eu/daily --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:eu-daily] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: JP Duties — MFN (Customs)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/jp-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:jp-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: JP Duties — FTA (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/jp-fta --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:jp-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: UK Duties — MFN
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/uk-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:uk-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: UK Duties — FTA
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/uk-fta --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:uk-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: ID Duties — MFN (official)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/id-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:id-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: ID Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["SG","MY","TH","PH","VN","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/id-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:id-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: MY Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["SG","TH","ID","PH","VN","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/my-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:my-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: PH Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["SG","MY","TH","ID","VN","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/ph-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:ph-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: CN Duties — MFN (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/cn-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:cn-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: CN Duties — FTA (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/cn-fta --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:cn-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: TH Duties — MFN (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/th-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:th-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: TH Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["SG","MY","ID","PH","VN","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/th-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:th-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: VN Duties — MFN (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/vn-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:vn-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: VN Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["SG","MY","TH","ID","PH","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/vn-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:vn-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: SG Duties — MFN (WITS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/sg-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:sg-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: SG Duties — FTA (WITS)
        run: |
          BODY='{"partnerGeoIds":["MY","TH","ID","PH","VN","BN","KH","LA","MM"]}'
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/sg-fta --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:sg-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: US Duties — MFN (HTS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/us-mfn --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:us-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: US Duties — FTA (HTS)
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/us-preferential --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:us-fta] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: MY Duties — MFN official Excel (if configured)
        if: ${{ env.MY_MFN_OFFICIAL_EXCEL_URL != '' }}
        run: |
          BODY=$(jq -n --arg url "$MY_MFN_OFFICIAL_EXCEL_URL" '{url: $url}')
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/my-mfn/official/excel --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:my-mfn-official] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: PH Duties — MFN official Excel (if configured)
        if: ${{ env.PH_TARIFF_EXCEL_URL != '' }}
        run: |
          BODY=$(jq -n --arg url "$PH_TARIFF_EXCEL_URL" '{url: $url}')
          OUT=$(bun run internal-request -- --path /internal/cron/import/duties/ph-mfn --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[duties:ph-mfn] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: UK Surcharges — remedies
        run: |
          BODY=$(jq -n --arg s "$UK_REMEDY_MEASURE_TYPES" '{measureTypeIds: ($s | split(",") | map(gsub("^\\s+|\\s+$"; "")) | map(select(length > 0)))}')
          OUT=$(bun run internal-request -- --path /internal/cron/import/surcharges/uk-remedies --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[surcharges:uk-remedies] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: EU Surcharges — remedies (when configured)
        if: ${{ env.EU_TARIC_REMEDY_TYPES != '' }}
        run: |
          BODY=$(jq -n --arg s "$EU_TARIC_REMEDY_TYPES" '{measureTypeIds: ($s | split(",") | map(gsub("^\\s+|\\s+$"; "")) | map(select(length > 0)))}')
          OUT=$(bun run internal-request -- --path /internal/cron/import/surcharges/eu-remedies --body "$BODY")
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[surcharges:eu-remedies] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: US Surcharges — all
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/import/surcharges/us-all --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[surcharges:us-all] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: De Minimis — official
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/de-minimis/import-official --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[de-minimis:import-official] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: De Minimis — Zonos
        run: |
          OUT=$(bun run internal-request -- --path /internal/cron/de-minimis/import-zonos --body '{}')
          echo "$OUT" | jq .
          ROWS=$(echo "$OUT" | jq -r '([.count,.inserted,.updated] | map(select(type=="number")) | add) // 0')
          if [ "$ROWS" -lt 1 ]; then
            echo "[de-minimis:import-zonos] expected >0 imported rows, got $ROWS"
            exit 1
          fi

      - name: Notices — CN (MOF)
        run: |
          bun run internal-request -- --path /internal/cron/notices/cn/mof --body '{}' | jq .

      - name: Notices — CN (GACC)
        run: |
          bun run internal-request -- --path /internal/cron/notices/cn/gacc --body '{}' | jq .

      - name: Notices — CN (MOFCOM)
        run: |
          bun run internal-request -- --path /internal/cron/notices/cn/mofcom --body '{}' | jq .

      - name: Prune old imports & provenance
        run: |
          BODY="{\"days\": ${PRUNE_DAYS}}"
          bun run internal-request -- --path /internal/cron/imports/prune --body "$BODY" | jq .

      - name: Slack (success)
        if: success()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -n "${SLACK_WEBHOOK_URL:-}" ]; then
            line1="✅ ${{ github.workflow }} succeeded in ${{ github.repository }} on \`${{ github.ref_name }}\`"
            payload=$(jq -n --arg line1 "$line1" --arg run "$RUN_URL" '{text: ($line1 + "\n" + $run)}')
            curl -fsS -X POST -H 'Content-type: application/json' --data "$payload" "$SLACK_WEBHOOK_URL"
          fi

      - name: Slack (failure)
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -n "${SLACK_WEBHOOK_URL:-}" ]; then
            line1="🚨 ${{ github.workflow }} failed in ${{ github.repository }} on \`${{ github.ref_name }}\`"
            payload=$(jq -n --arg line1 "$line1" --arg run "$RUN_URL" '{text: ($line1 + "\n" + $run)}')
            curl -fsS -X POST -H 'Content-type: application/json' --data "$payload" "$SLACK_WEBHOOK_URL"
          fi

      - name: Discord (success)
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -n "${DISCORD_WEBHOOK_URL:-}" ]; then
            content="✅ **${{ github.workflow }}** succeeded in **${{ github.repository }}** on \`${{ github.ref_name }}\`\n${RUN_URL}"
            payload=$(jq -n --arg content "$content" '{content: $content}')
            curl -fsS -H "Content-Type: application/json" -X POST -d "$payload" "$DISCORD_WEBHOOK_URL"
          fi

      - name: Discord (failure)
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -n "${DISCORD_WEBHOOK_URL:-}" ]; then
            content="🚨 **${{ github.workflow }}** failed in **${{ github.repository }}** on \`${{ github.ref_name }}\`\n${RUN_URL}"
            payload=$(jq -n --arg content "$content" '{content: $content}')
            curl -fsS -H "Content-Type: application/json" -X POST -d "$payload" "$DISCORD_WEBHOOK_URL"
          fi
